<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.doll.mapper.AppealMapper">





<!--    public BigDecimal getCommodityPrice(@Param("clawRecordId") Long clawRecordId);-->
    <select id="getCommodityPrice" resultType="com.doll.dto.AppealDto">
        SELECT  commodity.price price , capture_success.user_id userId  FROM  capture_success   LEFT JOIN commodity  ON
            capture_success.commodity_id=commodity.id  WHERE    capture_success.id= #{clawRecordId}

    </select>





</mapper>